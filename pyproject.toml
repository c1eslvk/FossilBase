[project]
name = "fossilbase"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Maksymilian Cieślak", email = "maks.cieslak13@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "folium>=0.20.0",
    "httpx>=0.28.1",
    "pydantic>=2.13.5",
]

[project.scripts]
fossilbase = "fossilbase.__main__:main"

[build-system]
requires = ["uv_build>=0.12.17,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
